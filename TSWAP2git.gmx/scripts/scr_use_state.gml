///scr_use_state()

/////     /////     ///// NEEDS  IMPROVEMENT /////     /////     /////
// check in circle shape for button
if (collision_circle(x,y,32,obj_use,false,true)!=noone) {
/*    var button=collision_circle(x,y,32,obj_use,false,true);
    var dir=point_direction(x,y,button.x,button.y);
// turn direction towards buttons, turn image_angle MORE towards button
    direction-=angle_difference(direction,dir)*turn*delta;
    image_angle-=angle_difference(image_angle,dir)*aim*delta;
// decide which arm is closer and use that arm. other one holds (lowers) gun
    if (point_distance()???
// all while also moving an arm (strechting x/y, moving/turning)

// return arm to start position
*/
// when done (or when no button nearby) } else {
} else {
    state=scr_move_state;
}
/////     /////     ///// NEEDS  IMPROVEMENT /////     /////     /////

